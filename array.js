var numbers =[2,2,3,4,4,5,6,7,8,10,10];
console.log(numbers);
for (let idea = 0; idea < numbers.length; idea++) {
    const element = numbers[idea];
    console.log(idea + "\t"+numbers[idea]);
    
}
for (let abc = 0; abc < numbers.length; abc++) {
    const element = numbers[abc];
  if (numbers[abc] <= 5 ) {
    
  }else(console.log(numbers[abc]))
}